<h1>Show Leave info</h1>

<h1><%= @leave.start_date %></h1>

<p><%= @leave.end_date %></p>

<ul>
  <li><%= link_to "Edit", edit_leafe_path(@leave) %></li>
  <li><%= link_to "Destroy", leafe_path(@leave),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %></li>
</ul>